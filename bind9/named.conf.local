// Removed the following line as rndc.key is already included in named.conf
// include "/etc/bind/rndc.key";

zone "." {
    type hint;
    file "/etc/bind/zones/db.root";
};

zone "example.com" {
    type master;
    file "/etc/bind/zones/db.example.com";
};
